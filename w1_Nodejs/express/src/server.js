// 引入内置模块
const path = require('path')

// 引入第三方模块
const express = require('express')

// 引入自定义模块
const utils = require('./utils')
const formatData = require('./utils/formatData')
const hello = require('./module')

console.log('utils',utils,hello)

